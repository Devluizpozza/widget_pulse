class PulseConfig {
  final bool enabled;
  final bool showOverlay;
  final bool enableHeatmap;

  const PulseConfig({
    this.enabled = true,
    this.showOverlay = true,
    this.enableHeatmap = false,
  });
}
